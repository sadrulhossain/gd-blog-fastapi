[project]
name = "gd-blog-fastapi"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "S Sadrul Hossain", email = "s.sadrul.hossain@portonics.com" }
]
requires-python = ">=3.13"
dependencies = [
    "aiosqlite>=0.22.1",
    "fastapi[standard]>=0.141.1",
    "greenlet>=3.5.5",
    "sqlalchemy>=2.0.52",
    "starlette>=1.5.0",
]

[project.scripts]
gd-blog-fastapi = "gd_blog_fastapi:main"

[build-system]
requires = ["uv_build>=0.12.3,<0.13.0"]
build-backend = "uv_build"
